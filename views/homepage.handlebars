<div class="container">
  <h1>Homepage</h1>
  {{#each posts}}
    <div class="post">
      <h2>{{ title }}</h2>
      <p>{{ content }}</p>
      <p>Posted by {{ username }} on {{ createdAt }}</p>
      <a href="/post/{{ id }}">Read More</a>
    </div>
  {{/each}}
</div>
